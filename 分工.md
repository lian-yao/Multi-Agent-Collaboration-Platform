# 四人团队分工

> 适用范围：AI Native 多智能体协作平台（2 周 / 10 个工作日交付）。
> 原则：三条验收链路（单 Agent 问答、多 Agent 协作流水线、故障恢复断点续跑）各有明确负责人；
> 代码目录按人划分，避免多人同时修改同一批文件。

## 1. 分工总览

| 成员 | 姓名 | 角色定位 | 主责代码目录 | 对应验收链路 | 报告章节 |
| --- | --- | --- | --- | --- | --- |
| 成员 A |  | 架构师 / 编排负责人 / 集成负责人 | `app/orchestration`、`app/agents` | 多 Agent 协作流水线 | 需求分析、架构设计（统稿） |
| 成员 B |  | Dapr / 基础设施工程师 | `app/workflows`、`app/core`、`deploy/`、存储配置 | 故障恢复断点续跑 | 架构设计、部署与演示 |
| 成员 C |  | 算法 / 模型与工具工程师 | `app/mcp`、`app/tools`、`app/sandbox`、`app/memory`、`app/observability` | 工具调用与记忆、多模型接入 | 核心模块说明（工具/记忆/模型） |
| 成员 D |  | 前端 / API 交付工程师 | `frontend/`、`app/api` | Web 可视化与端到端演示 | 核心模块说明（UI/API） |

## 2. 各成员职责

### 成员 A：架构师 / 编排负责人

- 设计 LangGraph 图结构、共享状态与节点边界。
- 实现任务规划与分解、多 Agent 协作流水线（信息收集 → 数据分析 → 报告生成）。
- 冻结编排层接口契约（消息结构、状态结构、工具调用记录）。
- 作为集成负责人，负责跨模块联调、代码合并前检查与演示彩排。
- 汇总各成员模块说明与验证结果，统稿项目报告。

### 成员 B：Dapr / 基础设施工程师

- 集成 Dapr Workflow 与 State Management，实现持久化执行与断点续跑。
- 配置 Redis、PostgreSQL 与 Dapr 组件（statestore、pubsub）。
- 支撑 LangGraph Checkpoint 与工作流状态快照落库。
- 维护 `deploy/` 一键启动/停止脚本，负责故障恢复演示（杀进程 → 重启 → 续跑）。

### 成员 C：算法 / 模型与工具工程师

- 接入 Ollama（默认）与 OpenAI（备选），管理模型参数。
- 设计 Agent 角色 Prompt 与示例场景数据。
- 实现 MCP 工具注册、发现与调用，完成计算器、搜索、代码执行、SQL 等至少 4 个内置工具。
- 实现会话记忆、长期记忆与工具执行沙箱隔离。
- 接入 OpenTelemetry + Jaeger + Prometheus，统计 Token 消耗与工具调用成功率。

### 成员 D：前端 / API 交付工程师

- 按 `doc/api.md` 与 A 冻结的接口契约实现 REST API。
- 开发 Web 控制台：Agent 团队状态、任务历史、工具调用链路、Token 统计、会话管理，
  以及多模态输入（图片 / 文档附件，ADR-021）与欢迎区任务原型卡（ADR-022）。
- 对接 API 完成端到端联调，录制演示并整理部署文档。

## 3. 按 10 天进度的分工节奏

| 阶段 | 成员 A | 成员 B | 成员 C | 成员 D |
| --- | --- | --- | --- | --- |
| D1–2：环境 + 单 Agent（基础已完成） | 复核图结构与状态设计 | 确保 Dapr CLI / Compose 环境稳定 | 选定并验证 Ollama 模型、多轮对话 | 跑通 API 链路、确认接口契约 |
| D3–4：Dapr 持久化 | 设计 LangGraph Checkpoint 接入点 | 实现 Workflow + State Management + 存储配置 | 定义会话与记忆数据结构 | 按冻结契约实现会话 API |
| D5–6：多 Agent 协作 | 设计流水线图与角色分配 | 封装可恢复的子任务 Workflow | 编写角色 Prompt、场景数据 | 搭建 Web 骨架 + 团队/任务状态页 |
| D7–8：工具与可观测 | 流水线接入 MCP 工具并验收 | 支持工具调用审计落库 | 完成内置工具、沙箱、可观测接入 | 展示调用链路与 Token 统计 |
| D9–10：集成交付 | 三条演示场景彩排、报告统稿 | 一键部署 + 断点续跑演示 | 端到端测试补缺、性能数据 | UI 收尾、录制演示、部署文档 |

里程碑：

- M1（D2）：单 Agent 问答可用。
- M2（D4）：Dapr 持久化与会话记忆完成。
- M3（D6）：多 Agent 流水线可演示。
- M4（D8）：工具生态与可观测性完成。
- M5（D10）：Web UI + 端到端测试 + 一键部署 + 报告交付。

## 4. 协作机制

1. **每日同步**：每天 15 分钟同步会，只汇报「已完成 / 今日计划 / 阻塞」，阻塞问题当场指定解决人。
2. **契约先行**：第 3 天前冻结消息结构、Workflow 状态、工具调用记录的 Pydantic Schema，写入代码，避免 D 端反复返工。
3. **目录隔离**：按第 1 节目录归属提交代码；跨目录改动需在合并前与归属人确认。
4. **集成把关**：A 负责整体集成，合并前运行 `uv run pytest`，确保相关测试通过。
5. **关键联调**：断点续跑是 A 与 B 的共同验收点，最迟 D6 做一次「杀进程 → 重启 → 续跑」演练。
6. **文档同步**：发现代码与 `doc/` 不一致时，先更新文档再改代码；重要决策写入 `doc/decisions/`。
7. **提交规范**：提交按逻辑单元拆分，commit message 说明原因；禁止破坏性 git 操作。
8. **跨目录改动的记录**：第 3 条要求跨目录改动先与归属人确认。本轮（D9-10 交付与之后的收尾）
   有若干处**未事先确认、事后登记**的跨边界改动，写在这里以便归属人复核。绝大多数是
   **纯新增文件或加带默认值的参数**（不影响既有行为）；改到既有行为的三处已在表内单独
   标明「改的是谁的什么」（ADR-023 的部署文件与沙箱模块、ADR-024 的存储策略反转、收尾一轮的
   附件列表查询去 `data` 列）：

   | 改动 | 落在谁的目录 | 性质 |
   | --- | --- | --- |
   | 新增动态编排图 `app/orchestration/dynamic_graph.py` | A | 纯新增文件；对静态链路只做了**加法**（`pipeline_graph.py` 增 `content_with_tools` / `invoke_role_messages` 两个公开薄封装，行为不变，既有用例全绿） |
   | 新增动态 Dapr 链路 `app/workflows/dynamic.py` | B | 纯新增文件；`workflows/pipeline.py` 只给 `WorkflowTask` 加了一个默认 `None` 的字段，`workflows/service.py` 只做加法注册与按模式选名（默认仍走 `agent_pipeline`） |
   | 新增 `GET /config/sandbox` 与「执行边界」分区 | C / D | 只读接口；`app/sandbox`、`app/tools` 未改动 |
   | 新增 `app/attachments/`（附件分类 / 抽取 / 提示词装配，ADR-021） | 未分配，邻接 C 的 `app/tools` | 纯新增目录；不依赖沙箱、不依赖工具注册表，也不改 `app/sandbox` 的既有策略 |
   | `app/core/checkpoint.py` 新增 `AttachmentRecord` 表与 7 个 CRUD | B | 纯加法：新增一个类与一组函数，既有表与查询未改语义 |
   | `app/workflows/pipeline.py` / `dynamic.py` 增 `attachment_ids` 透传 | B | 纯加法：`WorkflowTask` 加一个默认空列表字段，两个活动多一个可选参数 |
   | `app/orchestration/pipeline_graph.py` / `dynamic_graph.py` 增 `attachments` 参数 | A | 纯加法：新参数默认空；`build_human_content` 在**没有图片时返回纯字符串**，所以既有的无附件调用路径字节不变 |
   | `deploy/compose.yaml` backend 增挂 `/var/run/docker.sock` 与两个 `SANDBOX_*` 变量（ADR-023） | B | **改的是 B 的部署文件**：沙箱容器建在宿主机守护进程上，不挂套接字它在部署里必然不可用。安全代价与关闭方式写在 ADR-023 与 README；不挂即回落「不可用」，无其它行为变化 |
   | **ADR-036（Agent 模块化）跨线改动**：`app/agents/roles.py` 只读未改；`app/orchestration/pipeline_graph.py` 的 `role` 参数放宽为 `RoleId | str`（新增 `_role_key`，日志字段值不变）；`app/core/checkpoint.py` 加 `agent_registry.icon` 列、`update_agent_registry`、`seed_builtin_agents` 只回填空值 | C（checkpoint / pipeline_graph） | 加法 + 参数放宽；既有静态链路行为不变，908 项后端测试全绿 |
   | `app/sandbox/{config,runtime,docker_runtime}.py`、`app/api/main.py::_sandbox_availability`（ADR-023） | C | C 的模块：新增 `unavailable_reason()`、`cap_drop=['ALL']`、遮住 `/app` 的 tmpfs、镜像缺失的可行动错误。隔离参数**只增不减**，既有策略与拒绝行为未改 |
   | 附件改为全类型留原件（`app/attachments/prepare.py`、`_attachment_meta`、下载端点，ADR-024） | 未分配，邻接 C 的 `app/tools` | 存储策略**反转**：ADR-021 的「只有图片留字节」作废。`text_content` 语义与提示词链路未动 |
   | 新增 `app/tools/session_files.py`（会话级只读工具，ADR-025） | C | 纯新增文件；它是**旁路**而不是改造——`app/sandbox` 的策略与 `app/mcp/registry.py` 的既有注册表都没动，工具按会话临时包一层 |
   | `app/tools/config.py` 增 `session_files_enabled` / `session_file_max_chars` / `session_file_list_limit` | C | 纯加法：`ToolSettings` 加三个带默认值的字段，既有四个工具的配置未动 |
   | `app/mcp/registry.py` 增 `SessionFileRegistry` / `with_session_files` | C | 纯加法：新增装饰类；`build_tool_registry` 与 `tool_catalog` 的返回值未变（**会话级工具不进静态目录**，见 `doc/api.md` §5.3） |
   | `app/orchestration/tools.py` 增 `session_scoped_registry` | A | 纯加法：新增一个包装函数；既有 `AuditedToolRegistry` 链路未改语义 |
   | `app/workflows/pipeline.py` 的 `advance_pipeline_stage` 增 `session_id` 参数 | B | 参数默认 `None`；不传时行为与之前逐字一致（不包会话工具） |
   | `app/core/checkpoint.py` 的附件列表改走 `_attachment_meta_columns()`（ADR-024 遗留低效） | B | **改的是 B 的文件**：`list_attachments_for_messages` 不再加载 `data` / `text_content` 列，改为库侧 `data IS NOT NULL` 出 `has_original`。返回结构（含 `has_original`）与调用方口径**不变**，属纯性能改动 |
   | `app/attachments/extract.py` 增 PDF `/ToUnicode` 解码 | 未分配，邻接 C 的 `app/tools` | 抽取能力**增强**：能把嵌入子集字体的 CID 码解回文字。判据仍是「读不出就判 failed」，新增的是「字体码冲突时拒绝」而不是输出乱码 |
   | `scripts/make_attachment_fixtures.py`、`scripts/vision_eval.py`、`scripts/orchestration_ab.py` | 新增，不落在任何人的主责目录（`scripts/` 原属公用） | 纯新增；前两个生成的固件写入 `tests/fixtures/attachments/`，第三个只读 |
   | `app/mcp/registry.py` 增合成层 `RegistryServersToolRegistry`（ADR-026） | C | **改的是 C 的模块**：`build_tool_registry()` 从「基础后端」变成「基础后端 + 合成层」，因此 `GET /api/v1/tools` 的目录**从此包含登记 Server 已发现的工具**（之前漏了它们，`doc/api.md` §5.3 已同步）。`InstrumentedToolRegistry` / `SessionFileRegistry` 的语义与包裹顺序未变；`registry.ready` 日志不再调用 `list_tools()`（改为 `_base_tool_count()`：探测失败记 0，而不是把注册表构建整个带崩） |
   | `app/core/mcp_registry.py` 增 `_sync_orchestration_tools()`，在增删改 / 发现 / 列配置后调用 | B | **改的是 B 的文件**：只新增一个「记日志 + 通知编排层」的调用，五个函数的返回值与错误码未变。不做它则用户新建的 Server 要等进程重启才对 Agent 生效 |
   | `tests/unit/conftest.py` 增 `memory_mcp_registry`（autouse） | 测试基础设施（跨线） | 纯加法：把注册表 Server 快照固定为空，理由与既有 `memory_redis`、`MemoryMetricSink` 相同——**单测不连真实 PostgreSQL**。不隔离会让存储不可达时的用例卡满 TCP 超时（本轮实测：95 例从几十秒变成 15 分钟跑不完） |
   | `pyproject.toml` / `uv.lock` 新增 `pypdfium2==5.13.0`（ADR-027） | 环境与依赖 | 纯加法：BSD-3 / Apache-2.0 双许可，wheel 自带 pdfium 二进制，无系统依赖。**backend 镜像必须重建**（`uv sync --frozen`）才会带上；组件缺失时功能降级为 `failed` 并点明缺的是 `pypdfium2` |
   | 新增 `app/attachments/render.py`（PDF 页 → PNG） | 未分配，邻接 C 的 `app/tools` | 纯新增文件；**不改沙箱、不改工具注册表、不引 OCR**。渲染只作用于「抽不出正文的 PDF」，有文本层的 PDF 链路逐字未变 |
   | `app/attachments/{extract,prepare,prompt}.py` 接入页面图像（ADR-027） | 未分配，邻接 C 的 `app/tools` | 附件链路**增强**：`extract_pdf` 多一条「渲第一页探测」的出口；`load_payloads` 把这类 PDF 展开成逐页图片载荷；`prompt` 的附件计数改为按 id 去重、并把「没有可用正文」与「超出长度上限」拆成两句说明。`status` 语义变化（扫描件由 `failed` → `ready`）已写进 `doc/api.md` §5.16.1 |
   | 新增 `app/api/stage_trace.py`，**只读**引用 `app/workflows/state.py::read_step_result` | B（文件归 B，本次**未改动**） | 纯新增文件 + **只读导入**：B 的 `app/workflows/state.py` 一个字节没动，A 的 `app/orchestration/pipeline.py`（`PipelineState` / `deserialize_pipeline_state`）与 `pipeline_graph.py`（`role_for_stage`）同样只读。读的是 A/B 已经**写进状态存储**的阶段状态，不新增写入方、不改 schema、不触发阶段重跑。B 侧若要改 `state.py` 的 key 结构或 `_record_checkpoint` 的写入时机，需同步 `doc/api.md` §5.17 与 `app/api/stage_trace.py` |
   | `app/api/main.py` 增 `GET /api/v1/workflows/{id}/stages` 与三个响应模型 | 本线（D） | 本线目录内的新增路由；未改任何既有路由的入参、出参与状态码 |
   | `app/core/checkpoint.py` 增 `list_workflows_for_session()` | B | **只读新增**：一个新查询函数（按 `created_at` 升序），既有表结构与其它查询未动。排序不是风格而是契约——§5.18 的「对话 N」编号由列表位置决定，倒序会让用户回看时点错对话。B 若改 `workflow_runs` 的排序口径或分页语义，需同步 `doc/api.md` §5.18 |
   | `app/api/main.py` 增 `GET /api/v1/sessions/{id}/workflows` 与 `WorkflowListResponse`、`app/api/store.py` 增 `list_workflows()` | 本线（D） | 本线目录内的新增路由与存储方法；`SqlApiStore` 转调 B 的查询，`InMemoryApiStore` 同步补上同样排序（不然集成用例与线上行为不一致） |
   | `doc/api.md` §5.5 的指标标签口径改正 | 文档（跨线，原口径由 A/B/C 约定） | **修正文档漂移，不改行为**：原文写「labels.workflow_id（可选 agent_id/model）」，而编排层两条链路（`app/orchestration/{pipeline_graph,dynamic_graph}.py`）调 `observed_stage(...)` 时**只传 `role`**，采样里根本没有 `agent_id`。照原文实现的前端（只认 `agent_id`）会把三个 Agent 的 Token 合并成一个 `model` 分组。已把**实际标签集**与归集顺序（`agent_id`→`role`→`model`→任务级）写进 §5.5。**遗留**：`app/observability/{context,metrics}.py` 顶部注释仍引用旧口径（属 C 的目录，本次未动），建议 C 顺手同步一句

   | `app/core/agent_config.py` 增 `tool_names` 校验与 `resolve_agent_tools`、`app/core/checkpoint.py` 的 `agent_configs` 增列 | B | **改的是 B 的文件**：`OVERRIDE_FIELDS` 多一个字段（因此 `override_keys` 会多出 `tool_names`）、注册表迁移表多一列。既有六个字段的校验、写入与层级合并**逐字未动**；`tool_names` 刻意不进`resolve_agent_settings` 的合并链（见 `doc/api.md` §5.7）。B 若改 `agent_configs` 的列结构或`OVERRIDE_FIELDS`，需同步 ADR-035 与 §5.7 |
   | `app/orchestration/tools.py` 增 `ToolAllowlistRegistry` / `restricted_registry`、`app/workflows/{pipeline,dynamic}.py` 接白名单 | A / B | `tools.py` **纯新增**三个符号（异常、包装类、工厂函数），既有 `AuditedToolRegistry` 链路未改语义。两条链路各多一行调用：静态链路的 `advance_pipeline_stage` 增一个默认 `None` 的参数（不传时 `restricted_registry(registry, None)` 原样返回同一个对象，行为逐字一致），动态链路只改 `dynamic_step_activity` 内的一行收窄。**收窄必须发生在挂会话级文件工具之前**，顺序不能反 |
   | `app/api/main.py` 的 `WorkflowResponse` 增 `error` 字段 | 本线（D） | 本线目录内的响应模型补字段：`workflow_runs.error` 一直有写入方（`finalize_activity`），此前只是漏了响应模型，属**只读透传**，不改任何写入路径 |
   | `app/workflows/dynamic.py` 的 `dynamic_plan_activity` / `dynamic_progress_activity` 开写 `current_step` | B | **改的是 B 的文件**：动态链路此前**不写** `workflow_runs.current_step`（行与 checkpoint 两处都恒为 `None`），而前端只认「指针相等的那一步」——于是 §5.22「工具调用逐条长出」与 ADR-031「正在跑的那一步默认摊开」这两条判定对动态任务**永远为假**（`RunActivity.tsx` 的 `isLiveStep` / `isOpen`）。现在规划活动写首步、进度活动写下一步，终态归零；口径与静态链路 `_record_checkpoint` 一致（都在**上一步落盘时**把指针推到下一步）。改动只有 `update_workflow_run` 的 `current_step=` 参数与 `checkpoint.current_step` 的值（`None` → 步骤 id），其余字段、活动注册与调度顺序未动。详见 `doc/api.md` §5.22 第三条
   | `app/workflows/service.py` 注册 `dynamic_progress_activity`，并加一条扫描式守卫测试 | B | **改的是 B 的文件**：`dynamic.py` 新增的 `dynamic_progress_activity`（每步落盘进度、把 `current_step` 推到下一步）被 `agent_dynamic_workflow` 调用，而 `service.py::register()` 漏注册。Dapr 的「调用」与「注册」是**两份独立清单**（`ctx.call_activity(...)` vs `runtime.register_activity(...)`），漏注册在导入期与启动期**都不报错**，只在执行到那一步时失败（`Activity function named 'dynamic_progress_activity' was not registered!`）；动态链路的 `except` 又只把原因写进 `workflow_runs.error`，`docker logs` 里一行都不落。现象：动态链路（含单 Agent 动态问答）第 1 步跑完即整单 `failed`，界面上只说「失败」不说为什么。改动是两行（import + 一行 `register_activity`），另加守卫测试扫 `app/workflows/*.py` 里所有 `call_activity(...)` / `call_child_workflow(...)` 的名字，断言每一个都在已注册集合内——把这两份清单的漂移变成一次单测红灯，而不是一次线上静默失败。既有七个活动的注册、调度顺序与工作流本体未动 |

   决策记录：ADR-019（动态编排）、ADR-020（执行边界只读）、ADR-021（多模态附件）、
   ADR-022（欢迎区引导卡）、ADR-023（沙箱在部署环境启用）、ADR-024（附件原件留档）、
   ADR-025（会话级文件工具）、ADR-026（登记 Server 接入编排层）、
   ADR-027（扫描版 PDF 走页面图像，不引 OCR）、
   ADR-028（协作侧栏是「配置与用量画布」，执行轨迹归执行台）、
   ADR-029（角色图标按 `role` 解析，配置页与协作画布共用一处）、
   ADR-030（多智能体拓扑只做「计划驱动的 DAG」，六种拓扑其余留档 3）、
   ADR-035（角色工具白名单的接缝放在注册表层）；
   设计与档 3 路线：`doc/orchestration.md`。
   附件两处跨边界改动的验收见 `doc/testing.md` §4.4，D9-10 收尾一轮见 §4.5，
   MCP 注册表接入见 §4.6，扫描版 PDF 走页面图像见 §4.7，
   执行台弹窗改为「执行轨迹」与两处 UI 修正见 §4.8，
   侧栏改为协作画布与会话工作流编号回看见 §4.9，
   协作画布重写为节点图见 §4.10，
   节点图交互（浮层收进全屏 / 贴侧面 / 产出优先 / 去掉拖动）见 §4.11，
   浮层规则补全到工具链胶囊与两条容器层的账见 §4.12，
   角色图标按 role 解析与预览页在 file:// 下整页空数据的修复见 §4.13，
   多智能体拓扑支持面盘点与画布并行能力的可验证化见 §4.14，
   对话流 Markdown 正文与内联执行轨迹见 §4.15，
   运行期实时进度的两处连带缺陷（写 `current_step` + 画布规划节点）见 §4.17，
   角色工具白名单与记录页三处改版（就地展开日志、失败原因、指标按序列归集）见 §4.20。
   动态链路活动漏注册（两份清单漂移，整条链路在第一步后失败）与按源码反推的注册守卫见 §4.21。
   把远端 22 个提交拉进带在途改动的工作树（含两侧追加小节的定号）见 §4.23。
   搜索渠道从环境变量提升为运行期可写配置（新表 `tool_configs` + 工具层合并 + API + 配置页
   面板 + 部署默认值），见 `doc/testing.md` §4.28 与
   `doc/decisions/039-search-channel-runtime-config.md`；一键部署默认渠道改为本地网关，
   根因是部署默认值原先只落在被 gitignore 的 `deploy/.env` 里，同见 §4.28。

## 5. 项目报告写作分工

| 章节 | 负责人 |
| --- | --- |
| 需求分析 | A 主笔，C 补充算法需求 |
| 架构设计 | A + B |
| 核心模块说明 | 各模块负责人写各自部分 |
| 测试验证结果 | 每人补自己链路的测试与数据，A 汇总 |
| 部署与演示 | B + D |
| 统稿与格式 | A |

## 6. 风险与应对

| 风险 | 应对 |
| --- | --- |
| Dapr 与 LangGraph 集成复杂度高 | A/B 第 3–4 天集中攻坚，C/D 不同步依赖其完成 |
| 前端开发周期被压缩 | D 从 D5 开始并行开发，接口契约先行冻结 |
| 断点续跑联调不充分 | D6 前完成首次故障恢复演练，不拖到 D9 |
| 演示当天链路不稳定 | D9–10 不再加新功能，预留完整彩排与录屏时间 |
| 模型或环境不可用 | 默认 Ollama，OpenAI 备选；镜像拉取失败时按 ADR-003 走 GHCR |

## 7. 完成验收

- 三条演示链路可完整跑通：单 Agent 问答、多 Agent 协作、故障恢复断点续跑。
- 每个成员负责的模块均有测试覆盖，`uv run pytest` 通过。
- Web UI 可展示团队状态、任务历史与调用链路。
- Docker + Dapr Compose 可一键启动，报告各章节齐全。
