node_modules/
dist/

# 评审预览的构建产物（由 rendercheck/build-preview.py 生成，不入库）
rendercheck/ui-preview.html
rendercheck/.preview-bundle.js
rendercheck/.preview-bundle.css

# 冒烟用例的 esbuild 打包产物（2 MB，跑一次生成一次，不入库）
rendercheck/.smoke.cjs
